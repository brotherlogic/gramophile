apiVersion: batch/v1
kind: CronJob
metadata:
  name: gramophile-validator
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: gramophile-validator
              image: registry.brotherlogic-backend.com/brotherlogic/gramophile:0.54.0 # {"$imagepolicy": "flux-system:gramophile"}
          restartPolicy: OnFailure
